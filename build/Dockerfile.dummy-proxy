FROM alpine:latest
RUN apk add --no-cache socat
# Create a dummy UNIX socket and listen indefinitely so net.Dial succeeds
ENTRYPOINT ["/bin/sh", "-c", "mkdir -p /var/run/metal-proxy && rm -f /var/run/metal-proxy/metal.sock && socat UNIX-LISTEN:/var/run/metal-proxy/metal.sock,fork /dev/null"]
